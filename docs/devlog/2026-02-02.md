# 2026-02-02

## Yapılanlar
- Homebrew kuruldu ve PATH ayarlandı (.zprofile).

## Notlar / Sorunlar
- -

## Sonraki Adım
- Git kurulumu ve ayarları

## Güncelleme
- Proje adı `yasalzeka` → **Akıllı Hukuk Asistanı** olarak değiştirildi.
- Proje klasörü `akilli-hukuk-asistani` olarak yeniden adlandırıldı.

## GitHub
- SSH bağlantısı doğrulandı ve known_hosts'a eklendi.
- Remote repo bağlandı ve ilk push yapıldı.

## Altyapı: PostgreSQL
- Docker Desktop kuruldu ve doğrulandı (docker + compose).
- docker-compose ile PostgreSQL (postgres:16-alpine) ayağa kaldırıldı.
- DB: akilli_hukuk, user: akilli, port: 5432.

## Altyapı: MLflow
- MLflow tracking server docker-compose'a eklendi.
- Port çakışması nedeniyle host portu 5050 olarak ayarlandı.
- UI: http://localhost:5050

## Altyapı: MLflow
- MLflow tracking server docker-compose'a eklendi.
- Port çakışması nedeniyle host portu 5050 olarak ayarlandı.
- UI: http://localhost:5050

## Altyapı: MLflow Entegrasyonu
- MLflow servis ayağa kaldırıldı (UI: http://localhost:5050).
- API içine MLflow client eklendi ve /mlflow/test-run endpoint'i ile ilk run loglandı.
- Tracking URI (container içi): http://mlflow:5000

## MLflow Param Log Fix
- API container'a data klasörü read-only volume olarak mount edildi.
- DVC md5 fingerprint artık container içinde okunup MLflow params olarak loglanıyor.

## Milestone: Local Altyapı Tamam
- Docker: postgres + api + mlflow + minio çalışıyor.
- DVC: localstore ve s3store (MinIO) remote ayarlandı.
- MLflow: API üzerinden test-run çalışıyor ve run üretiyor.
